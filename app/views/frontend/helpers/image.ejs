<%
    imageHelper = (folder, imageName, alt = "Image", idName="img-responsive img-avatar") => {
        if (imageName == null || imageName == "") {
            imageName = 'no-avatar.jpg';
        }
        return `<img id="${idName}" src="${folder + imageName}" width="100%" height="5%" alt="${alt}" />`;
    }
%>